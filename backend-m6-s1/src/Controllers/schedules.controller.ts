// import { Request, Response } from "express";
// import {
//   createScheduleService,
//   listScheduleService,
// } from "../Services/schedules.services";

// export const createScheduleController = async (
//   request: Request,
//   response: Response
// ) => {
//   const newSchedule = await createScheduleService(request);
//   return response.status(201).send({ message: "Agendado!" });
// };

// export const listScheduleController = async (
//   request: Request,
//   response: Response
// ) => {
//   const schedules = await listScheduleService(request);
//   return response.status(200).json(schedules);
// };
